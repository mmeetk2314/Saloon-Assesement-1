@page
@model Saloon_Assesement_1.Pages.Parlours.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Parlour[0].Parlour_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Parlour[0].Parlour_Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Parlour[0].Contact_Number)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Parlour) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Parlour_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Parlour_Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Contact_Number)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Parlour_ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Parlour_ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Parlour_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
